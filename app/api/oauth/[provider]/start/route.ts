import {NextResponse} from "next/server";import {buildAuthorizationUrl,createState,oauthConfigured,providerConfig,type OAuthProvider}from"@/lib/oauth";export async function GET(request:Request,{params}:{params:Promise<{provider:string}>}){const provider=(await params).provider as OAuthProvider;if(!(provider in providerConfig))return NextResponse.json({error:"Unsupported provider"},{status:404});if(!oauthConfigured(provider))return NextResponse.redirect(new URL(`/connect?error=${provider}_oauth_not_configured`,request.url));const state=await createState(provider);return NextResponse.redirect(buildAuthorizationUrl(provider,state))}
