import {NextResponse} from "next/server";import {getWorkspaceStore} from "@/lib/db";import {workflowSchema} from "@/lib/validation/schemas";export async function GET(){const s=await getWorkspaceStore();return NextResponse.json({workflows:await s.workflows.list()})}export async function POST(r:Request){try{const p=workflowSchema.safeParse(await r.json());if(!p.success)return NextResponse.json({error:"Invalid workflow"},{status:400});const s=await getWorkspaceStore();return NextResponse.json({workflow:await s.workflows.create({id:p.data.id??`workflow_${crypto.randomUUID()}`,name:p.data.name,platform:p.data.platform,description:p.data.description})},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Request failed"},{status:400})}}
