import Link from "next/link";import {notFound} from "next/navigation";import {getWorkspaceStore} from "@/lib/db";export const dynamic="force-dynamic";export default async function IncidentDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const store=await getWorkspaceStore();const incident=await store.incidents.get(id);if(!incident)return notFound();const workflow=await store.workflows.get(incident.workflowId);const event=await store.events.list(incident.workflowId);const execution=event.find(e=>e.id===incident.eventId);return <div className="detail-page"><Link className="back-link" href="/incidents">← Findings</Link><div className="detail-hero"><div><span className="finding-status large">{incident.status.toUpperCase()} · {incident.severity.toUpperCase()}</span><h1>{incident.title}</h1><p>{incident.summary}</p></div><div className="detail-impact"><span>IMPACT</span><strong>{incident.impact}</strong></div></div><section className="proof-chain"><div><span>01</span><b>Automation execution</b><strong>SUCCESS</strong><small>{workflow?.name} · {execution?.executionId || "unknown"}</small></div><div><span>02</span><b>Business expectation</b><strong>{incident.expected}</strong><small>Contract-backed requirement</small></div><div><span>03</span><b>Observed state</b><strong className="metric-red">{incident.observed}</strong><small>Read-only downstream verification</small></div><div><span>04</span><b>Conclusion</b><strong>OUTCOME MISMATCH</strong><small>Evidence-backed finding</small></div></section><section className="evidence-card"><div className="section-heading"><span>EVIDENCE GRAPH</span><h2>Why Outcom opened this finding</h2></div><div className="evidence-list">{incident.evidence.map((x,i)=><div key={i}><span>{String(i+1).padStart(2,"0")}</span><p>{x}</p></div>)}</div><div className="recommendation"><span>RECOMMENDED ACTION</span><p>{incident.recommendedAction}</p></div></section></div>}
