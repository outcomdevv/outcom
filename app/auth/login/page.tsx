import Link from "next/link";
export default function Login({searchParams}:{searchParams:Promise<{error?:string;notice?:string}>}){return <div className="auth-page"><div className="auth-card"><img src="/outcom-logo.png" className="auth-logo" alt="Outcom"/><span className="auth-kicker">AUTOMATION ASSURANCE PLATFORM</span><h1>Protect the outcome,<br/><em>not just the run.</em></h1><p>Sign in to your Outcom workspace and verify business state across your automation fleet.</p><AuthForm searchParams={searchParams}/></div></div>}
async function AuthForm({searchParams}:{searchParams:Promise<{error?:string;notice?:string}>}){const p=await searchParams;return <><form action="/api/auth/login" method="post"><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{p.error&&<div className="auth-error">{p.error}</div>}{p.notice&&<div className="auth-notice">{p.notice}</div>}<button className="auth-submit">Sign in <span>↗</span></button></form><div className="auth-divider"><span>New to Outcom?</span></div><Link className="auth-secondary" href="/auth/signup">Create workspace</Link></>}
